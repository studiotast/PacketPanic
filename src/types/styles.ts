import type { CSSProperties } from "react";

/**
 * Types for the SCSS/CSS modules in this project
 */
export declare type ClassNames = {
  readonly [key: string]: string;
};

/**
 * Generic utility types for the styling in this project
 */
export declare type StyleObject = CSSProperties;
